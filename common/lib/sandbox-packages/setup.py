from setuptools import setup

setup(
    name="sandbox-packages",
    version="0.2.42",
    packages=[
        "loncapa",
        "verifiers",
        "hint",
    ],
    py_modules=[
        "eia",
    ],
    install_requires=[
    ],
)
