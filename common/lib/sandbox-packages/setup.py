from setuptools import setup

setup(
    name="sandbox-packages",
    version="0.1.5",
    packages=[
        "loncapa",
        "verifiers",
        "ply",
        "expr_parser",
        "cluster",
    ],
    py_modules=[
        "eia",
    ],
    install_requires=[
    ],
)
