from setuptools import setup

setup(
    name="sandbox-packages",
    version="0.1.1",
    packages=[
        "loncapa",
        "verifiers",
        "expr_parser",
    ],
    py_modules=[
        "eia",
    ],
    install_requires=[
    ],
)
