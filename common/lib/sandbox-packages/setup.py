from setuptools import setup

setup(
    name="sandbox-packages",
    version="0.2.13",
    packages=[
        "loncapa",
        "verifiers",
        "expr_parser",
        "hint",
    ],
    py_modules=[
        "eia",
    ],
    install_requires=[
    ],
)
