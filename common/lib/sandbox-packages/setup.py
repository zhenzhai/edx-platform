from setuptools import setup

setup(
    name="sandbox-packages",
    version="0.2.8",
    packages=[
        "loncapa",
        "verifiers",
        "expr_parser",
        "hint",
    ],
    py_modules=[
        "eia",
    ],
    install_requires=[
    ],
)
