from setuptools import setup

setup(
    name="sandbox-packages",
    version="0.1.8",
    packages=[
        "loncapa",
        "verifiers",
        "expr_parser",
        "cluster",
    ],
    py_modules=[
        "eia",
    ],
    install_requires=[
    ],
)
